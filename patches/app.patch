diff --git a/apps/tutorial_app_v2ml.cpp b/apps/tutorial_app_v2ml.cpp
index 08c0b84..7e754a3 100644
--- a/apps/tutorial_app_v2ml.cpp
+++ b/apps/tutorial_app_v2ml.cpp
@@ -366,7 +366,21 @@ int main(int argc, char** argv)
     std::string pre_dir = model_dir + "/preprocess";
 
     /* Input image file */
-    std::string filename = "sample.bmp";
+    std::string filename;
+    if (argc == 1)
+    {
+        filename = "sample.bmp";
+    }
+    else if (argc == 2)
+    {
+        filename = std::string(argv[1]);
+    }
+    else
+    {
+        std::cerr << "Invalid number of arguments " << std::endl;
+        return -1;
+    }
+    std::cout << "Processing: " << filename << std::endl;
 
     /* About u-dma-buf
         Pre-processing Runtime requires the input buffer to be allocated in continuous memory area.
